Contribution guidance for this repository lives in AGENTS.md, so that every coding assistant
reads the same instructions. Claude Code reads CLAUDE.md rather than AGENTS.md, so this file
just imports it:

@AGENTS.md
